---
title: Waves list
includePagination: true
pagination:
  data: collections.all
  size: 21
  alias: waves
  reverse: true
layout: simple
permalink: /waves/{% if pagination.pageNumber > 0 %}{{ pagination.pageNumber }}/{% endif %}
---
<table>
  <thead>
    <tr>
      <th>URL</th>
      <th>Заголовок</th>
    </tr>
  </thead>
  <tbody>
{%- for wave in waves %}
  <tr>
    <td><a href="{{ wave.url }}"><code>{{ wave.url }}</code></a></td>
    <td>{{ wave.data.title }}</td>
  </tr>
{%- endfor %}
  </tbody>
</table>